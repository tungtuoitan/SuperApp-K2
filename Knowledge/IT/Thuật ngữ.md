---
id: 31
name: "Thuật ngữ"
---

# Tree-shaking là gì? [id:195 order:1]
Tree-shaking là kỹ thuật trong build (Webpack, Rollup, Vite…) dùng để loại bỏ code không được sử dụng khỏi bundle cuối.

# Distributed là gì? [id:193 order:2]
là phân tán, đối lập với centralized

# Circular dep là gì? [id:196 order:2]
là A import B và B import A

# fine-grained import là gì? [id:203 order:7]
laf import đúng phần cần dùng

# cluster là gì? [id:209 order:13]
là  nhóm các module liên quan chặt chẽ với nhau

# backfill là gì? [id:246 order:27]
là điền data cũ vào bảng mới tạo

# phân biệt derived và stored state? [id:248 order:29]
là state được tính toán từ stored state

# IIFE là gì ? [id:249 order:30]
Immediately Invoked Function Expression — 
tức là “hàm được định nghĩa và chạy ngay lập tức

# Audit Trail là gì? [id:251 order:31]
là 1 loại log có structure, thường lưu ở DB

# Derived state khác gì so với computed state? [id:271 order:32]
gần như là giống nhau

# Derived đọc thế nào? [id:272 order:33]
đì rai vd

# Coarse-grained import là gì? [id:276 order:37]
là import nguyên 1 thư viện vào, mặc dù chỉ dùng 1 hàm

# Hook nghĩa gốc là gì? [id:277 order:38]
là cái móc

# Hook trong react là gì? [id:278 order:39]
là hàm đặc biệt, dùng để MÓC vào component

# revert và reverse khác nhau gì? [id:279 order:40]
đều là quay lại, 
còn reverse nhấn mạnh hành động đảo ngược hành động trước đó
còn revert thì không